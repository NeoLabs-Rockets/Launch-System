# NeoLabs Mission Dashboard — environment config
# Copy this file to .env and fill in values. .env is git-ignored.

# HTTP Basic Auth password for public/cloud deployment.
# Any username is accepted; only the password is checked.
# Leave blank (or remove the line entirely) for local LAN use — auth is
# fully disabled when this variable is not set or is empty.
DASHBOARD_PASSWORD=
